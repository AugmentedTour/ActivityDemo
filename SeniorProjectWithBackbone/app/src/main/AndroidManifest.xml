<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.robin.seniorprojectwithbackbone">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name=".PermissionsAndStart">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".BuildingSelect"/>
        <activity android:name=".Example" />
        <activity android:name=".RBuilding" />
        <activity android:name=".ArtsAndHumanitiesMain" />
        <activity android:name=".HelpActivity"></activity>
    </application>

    <!-- this is bad practice, we'll want to change this out for an actual internet permission ask thing -->
    <uses-permission android:name="android.permission.INTERNET" />

</manifest>