<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.robin.seniorprojectwithbackbone">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name=".PermissionsAndStart">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".BuildingSelect"/>
        <activity android:name=".HelpActivity" />
        <activity android:name=".Example" />
        <!-- F building -->
        <activity android:name=".FBuilding" />
        <!-- G building -->
        <activity android:name=".GBuilding" />
        <!-- Housing building -->
        <activity android:name=".HousingBuilding" />
        <activity android:name=".HousingGeneralInformation" />
        <activity android:name=".HousingAmeneties" />
        <!-- K building -->
        <activity android:name=".KBuilding" />
        <!-- L building -->
        <activity android:name=".LBuilding" />
        <activity android:name=".LScienceDivisionActivity" />
        <activity android:name=".LInteriorDesignActivity" />
        <!-- R building -->
        <activity android:name=".RBuilding" />
        <activity android:name=".RArtsAndHumanitiesMain" />
        <activity android:name=".RArtsAndHumanitiesDepartments" />
        <activity android:name=".RDanceMain" />
        <activity android:name=".RELIMain" />
        <activity android:name=".RELIPrograms" />
        <activity android:name=".RABEMain" />
        <!-- S building -->
        <activity android:name=".SBuilding" />
        <activity android:name=".SScienceDepartments" />
        <!-- U building -->
        <activity android:name=".UBuilding" />

    </application>

    <!-- this is bad practice, we'll want to change this out for an actual internet permission ask thing -->
    <uses-permission android:name="android.permission.INTERNET" />

</manifest>